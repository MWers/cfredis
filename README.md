# cfredis #

cfredis is a [Redis](http://redis.io/) client for ColdFusion. Technically, it is a wrapper for the excellent Redis java client [Jedis](https://github.com/xetorthio/jedis/). It converts Jedis' arguments and return values into ColdFusion equivalents. 